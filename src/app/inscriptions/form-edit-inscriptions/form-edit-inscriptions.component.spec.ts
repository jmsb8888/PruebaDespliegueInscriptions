/*import { ComponentFixture, TestBed } from '@angular/core/testing';
import {FormEditInscriptionsComponent} from './form-edit-inscriptions.component';

describe('FormEditInscriptionsComponent', () => {
  let component: FormEditInscriptionsComponent;
  let fixture: ComponentFixture<FormEditInscriptionsComponent>;

  beforeEach(async () => {
    await TestBed.configureTestingModule({
      declarations: [ FormEditInscriptionsComponent ]
    })
    .compileComponents();

    fixture = TestBed.createComponent(FormEditInscriptionsComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});*/
